<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.silver.lwb.dao.RolerProfileDao">
    <select id="inquireRolerName" resultType="STRING">
        SELECT rname FROM roler_profile
        WHERE rid = #{rID}
    </select>
</mapper>